import { Link } from "expo-router"
import { Text, View } from "react-native"

const SettingUser = () => {
    return (
        <View style={{
            paddingTop: 50,
            flexDirection: 'row',
        }}>
        </View>
    )
}

export default SettingUser